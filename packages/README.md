# RoomForge Shared Packages

This directory holds shared schemas and token exports consumed by the Flutter app, Three.js editor, and FastAPI server.

Current shared source:

- `design-tokens.json`: candidate, confirmed, selected, warning, error, measurement, save, and admin status tokens.
